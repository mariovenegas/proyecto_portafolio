from email.policy import default
from ssl import create_default_context
from typing_extensions import runtime
from django.db import models
# from services.models import Service
# from contractsimport import Contract

# Create your models here.

# class Contractdetail(models.Model):
  #  contract = models.ForeignKey(Contract, on_delete=models.CASCADE, null=True)
  #  service = models.ForeignKey(Service, on_delete=models.CASCADE, null=True)
  # created = models.DateTimeField(auto_now_add=True, verbose_name="Fecha de creación")
  #  updated = models.DateTimeField(auto_now=True, verbose_name="Fecha de actualización", null=True)

 #   class Meta:
 #      verbose_name = "detallecontrato"
 #       verbose_name_plural = "detallecontratos"
 #      ordering = [ '-created']
    
